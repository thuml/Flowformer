from setuptools import setup

setup(name='flowformerGPT',
      version='0.0.1',
      author='thuml',
      packages=['flowformergpt'],
      description='A PyTorch re-implementation of FlowFormerGPT',
      license='thuml',
      install_requires=[
            'torch',
      ],
)
